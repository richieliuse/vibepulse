import Foundation

// Claude Code spawns this hook. The sample writer lands with VibePulseState.
@main
enum StatuslineMain {
    static func main() {
        fputs("vibepulse-statusline: writer not installed yet\n", stderr)
        exit(0)
    }
}
